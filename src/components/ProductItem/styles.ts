import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  pane: {
    marginBottom: 15,
  },

  dataWrapper: {
    flexDirection: 'row',
    alignItems: 'center',
  },

  image: {
    height: 100,
    width: 100,
    marginRight: 10,
  },

  controls: {
    marginTop: 10,
    marginLeft: 110,
  },
});

export default styles;
