import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonsWrapper: {
    width: '100%',
    flexDirection: 'row',
  },

  button: {
    flex: 1,
    paddingLeft: 10,
    paddingRight: 10,
  },
});

export default styles;
