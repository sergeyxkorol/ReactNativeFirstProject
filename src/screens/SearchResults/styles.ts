import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  searchWrapper: {
    position: 'absolute',
    top: 0,
    width: '100%',
    zIndex: 10,
  },

  productWrapper: {
    marginTop: 86,
    paddingBottom: 80,
  },
});

export default styles;
